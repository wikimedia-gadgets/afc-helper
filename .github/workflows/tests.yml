name: Tests

on: [push, pull_request]

jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - run: npm install
    - name: Run linter
      run: npm run lint
    - name: Run unit tests
      run: npm test
    - name: Test run the build
      run: grunt build
